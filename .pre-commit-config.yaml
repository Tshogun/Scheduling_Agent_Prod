repos:
  - repo: local
    hooks:
      - id: lint-staged
        name: Run lint-staged in frontend folder
        entry: npm exec lint-staged
        language: node
        pass_filenames: false
        # Run only on files inside frontend
        files: ^frontend/
