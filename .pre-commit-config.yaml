repos:
  - repo: local
    hooks:
      - id: lint-staged
        name: Run lint-staged in frontend folder
        entry: npm exec -- node -e "console.log('cwd:', process.cwd())" && npm exec lint-staged
        language: node
        pass_filenames: false
        files: ^frontend/
