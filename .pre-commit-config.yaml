repos:
  - repo: local
    hooks:
      - id: lint-staged
        name: Run lint-staged on frontend files
        entry: cmd /c "cd frontend && npm exec lint-staged"
        language: system
        pass_filenames: false
        files: ^frontend/

  - repo: local
    hooks:
      - id: golangci-lint
        name: golangci-lint for go-orchestrator
        entry: bash -c "cd backend/services/go-orchestrator && golangci-lint run"
        language: system
        types: [go]
        pass_filenames: false

  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.12.10
    hooks:
      # Run the linter.
      - id: ruff-check
        types_or: [ python, pyi ]
        args: [ --fix ]
      # Run the formatter.
      - id: ruff-format
        types_or: [ python, pyi ]

  - repo: https://github.com/psf/black
    rev: 25.1.0  # Use the latest stable version
    hooks:
      - id: black
        name: black (formatter)
        language_version: python3
        args: [./backend/services/python-services]

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.17.1  # Use a compatible version of mypy
    hooks:
      - id: mypy
        name: mypy (type checker)
        args: [./backend/services/python-services]
        additional_dependencies: []  # Add e.g. ['types-requests'] if needed
