repos:
  - repo: local
    hooks:
      - id: lint-staged
        name: Run lint-staged script on Windows
        entry: run-lint-staged.bat
        language: system
        pass_filenames: false
        files: ^frontend/
